Django==5.1.4
django-ninja==1.3.0
psycopg[binary]==3.2.3
django-environ==0.11.2
django-cors-headers==4.6.0
PyJWT==2.10.1
openpyxl==3.1.5
celery==5.4.0
django-celery-beat==2.7.0
redis==5.2.0
django-storages[s3]==1.14.4
boto3==1.35.49
whitenoise==6.8.2
httpx==0.27.2
# Google Sheets ingest (wellness Check-IN form responses → ExamResults).
gspread==6.1.4
google-auth==2.35.0
# PDF report generation:
#   Pillow — required by Django's ImageField (Club.logo) + matplotlib's
#   PNG export. reportlab — scaffold (cover, sections, footer, paginated
#   layout). matplotlib — chart renderers from resolver payloads.
Pillow==10.4.0
reportlab==4.2.5
matplotlib==3.9.2
# LLM narrative for the player Resumen report (player_triage). Optional at
# runtime — the report degrades gracefully to tables-only when no API key is
# configured. `>=` so pip pulls a version new enough for adaptive thinking.
anthropic>=0.70.0
# Editable Word (.docx) report export (dashboards/docx). Reuses the same
# payloads + narrative + matplotlib charts as the PDF builders.
python-docx==1.1.2
