import type { Metadata } from "next";

export const metadata: Metadata = { title: "Configuraciones" };

export default function ConfiguracionesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
